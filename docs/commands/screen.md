# Screen

The cloud servers are started in an internal process. The system output is shown in this screen.

## Commands

* `screen list`
* `screen join <name>`
* `screen leave`
* `screen toggle <name>`
* `screen exec <command...>`
